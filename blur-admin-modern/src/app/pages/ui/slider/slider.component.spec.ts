import { TestBed } from '@angular/core/testing';
import { SliderComponent } from './slider.component';
describe('SliderComponent', () => { beforeEach(() => TestBed.configureTestingModule({ imports: [SliderComponent] })); it('creates and renders all slider demos', () => { const f = TestBed.createComponent(SliderComponent); f.detectChanges(); expect(f.componentInstance).toBeTruthy(); expect(f.nativeElement.querySelectorAll('.slider-box').length).toBe(8); expect(f.nativeElement.querySelectorAll('ba-slider').length).toBe(6); expect(f.nativeElement.querySelectorAll('.slider-box > ngx-slider').length).toBe(2); expect(f.nativeElement.textContent).toContain('Decorating numbers'); expect(f.componentInstance.decoratedOptions.translate?.(300000, 0 as never)).toBe('300.000'); expect(f.componentInstance.monthOptions.translate?.(5, 0 as never)).toBe('June'); }); });
